# ============================================================
# CommCoach — Environment Variables Template
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit real secrets.
# ============================================================

# ------------------------------------------------------------
# Database (Turso / libsql)
# Local dev: keep as file:./dev.db (no other changes needed)
# Production: set to your Turso libsql:// URL
# ------------------------------------------------------------
DATABASE_URL="file:./dev.db"

# Only required when DATABASE_URL is a Turso libsql:// URL
# Get from: turso db tokens create <db-name>
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# ------------------------------------------------------------
# NextAuth
# ------------------------------------------------------------
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-this-with-a-random-secret"

# Base URL of your app (no trailing slash)
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ------------------------------------------------------------
# Google OAuth
# From: Google Cloud Console > APIs & Services > Credentials
# > OAuth 2.0 Client IDs > Web application
#
# Authorized redirect URIs to add:
#   http://localhost:3000/api/auth/callback/google
#   https://your-app.vercel.app/api/auth/callback/google
# ------------------------------------------------------------
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ------------------------------------------------------------
# Anthropic Claude API
# Required for: AI Story Generator, Mock Interview, Storytelling Exercises, Transcript Analysis
# Get from: https://console.anthropic.com/api-keys
# ------------------------------------------------------------
ANTHROPIC_API_KEY="sk-ant-..."
